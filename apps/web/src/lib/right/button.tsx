import { defineComponent } from "vue";

export const ButtonConfig = defineComponent({
  name: "ButtonConfig",
  setup: () => {
    return () => {
      return <div>ButtonConfigButtonConfigButtonConfig</div>;
    };
  },
});
